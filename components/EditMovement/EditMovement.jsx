import React, {useState, useEffect} from 'react';
import styles from './EditMovement.module.scss';
import button from '../Button'

const EditMovement = ({movementId, movementName, setId, apiUrl}) => {
    return <div></div>
}

export default EditMovement